<base:BasePage
    x:Class="UnoApp.Presentation.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:base="using:UnoApp.Presentation.Common.Presentation"
    xmlns:local="using:UnoApp.Presentation"
    xmlns:models="using:UnoApp.Models"
    xmlns:uen="using:Uno.Extensions.Navigation.UI"
    xmlns:constants="using:UnoApp.Constants"
    xmlns:utu="using:Uno.Toolkit.UI"
    NavigationCacheMode="Required">

    <Grid uen:Region.Attached="True">

        <NavigationView x:Name="NavView" 
                        SelectionChanged="NavigationView_SelectionChanged"
                        IsBackButtonVisible="Collapsed">
            <NavigationView.MenuItems>
                <NavigationViewItem Content="One" Tag="One" />
                <NavigationViewItem Content="Two" Tag="Two" />
                <NavigationViewItem Content="Three" Tag="Three" />
                <NavigationViewItem Content="Kontakte" Tag="{x:Bind constants:PageNames.Contacts}" />
                <NavigationViewItem Content="Wix-Kontakte" Tag="{x:Bind constants:PageNames.WixContacts}" />
            </NavigationView.MenuItems>

            <Grid>
                <!-- Static content areas -->
                <Grid x:Name="OneContent" Visibility="Collapsed">
                    <TextBlock Text="One Content" HorizontalAlignment="Center" VerticalAlignment="Center" />
                </Grid>
                <Grid x:Name="TwoContent" Visibility="Collapsed">
                    <TextBlock Text="Two Content" HorizontalAlignment="Center" VerticalAlignment="Center" />
                </Grid>
                <Grid x:Name="ThreeContent" Visibility="Collapsed">
                    <TextBlock Text="Three Content" HorizontalAlignment="Center" VerticalAlignment="Center" />
                </Grid>
                
                <!-- Dynamic content for Contacts and WixContacts -->
                <ContentControl x:Name="DynamicContent" 
                                uen:Region.Attached="true"
                                HorizontalAlignment="Stretch"
                                VerticalAlignment="Stretch"
                                HorizontalContentAlignment="Stretch"
                                VerticalContentAlignment="Stretch"
                                Visibility="Collapsed"/>
            </Grid>
        </NavigationView>
    </Grid>
</base:BasePage>
